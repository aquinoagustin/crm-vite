Loader
Se utilizan los Loaders para obtener datos de una API o de un objeto (Similares a un state)








Action
Se utilizan para procesar la entrada de datos de un Form

Componente Form, instalacion y uso

Componente donde utilizamos el formulario, para usar el form en las nuevas versiones, sin necesidad de hacer un onSubmit

import {Form} from 'react-router-dom';

export function action(){

	console.log('Enviando formulario');
}


<Form method="post" >
	.....
	.....

<input type="submit"/>

</Form>


En el main.jsx, donde tenemos las rutas importamos el action del componente y lo añadimos, ejemplo:

import {action as nuevoClienteAction} from './pages/NuevoCliente';

{
   path:'/clientes/nuevo',
   element:<NuevoCliente/>,
   action:nuevoClienteAction
}
